# API base URL for production (leave empty to use relative /api path)
VITE_API_BASE_URL=

# Logo image URL (falls back to default ImageKit URL if not set)
VITE_LOGO_URL=

# Dev proxy target (only used in local development via vite.config.ts)
# VITE_API_PROXY_TARGET=http://localhost:5143
